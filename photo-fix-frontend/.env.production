# Used automatically by `npm run build` (Vite loads .env.production for
# production builds). This value gets baked into the JS bundle at build time —
# changing it later requires rebuilding and re-uploading, it cannot be edited
# after the fact like a normal server .env file.
VITE_API_URL=https://api.pixelgraphicstudio.com/api/v1
